// removed — MetalE2ETests referenced RainbowRenderer / TextRenderer from the
// SwiftUI Metal UI layer which was removed. Terminal UI (Spinner, ProgressBar,
// MarkdownRenderer) is tested in UITests/.
