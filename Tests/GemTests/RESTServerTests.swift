// removed — RESTServerTests used AuthService / GemmaRequestContext / User types
// that were removed when JWT auth was dropped. Controllers are now tested via
// integration (curl) and OrchestratorTests covers the core logic.
